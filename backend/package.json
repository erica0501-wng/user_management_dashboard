{
  "name": "user_management_dashboard",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "node src/server.js",
    "dev": "node src/server.js",
    "studio": "npx prisma studio --schema=./prisma/schema.local.prisma",
    "db:view": "node view-portfolio-database.js",
    "vercel-build": "npx prisma migrate deploy --schema=./prisma/archive/schema.vercel.prisma && npx prisma generate --schema=./prisma/archive/schema.vercel.prisma",
    "build": "npx prisma generate --schema=./prisma/archive/schema.vercel.prisma",
    "postinstall": "npx prisma generate --schema=./prisma/archive/schema.vercel.prisma",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "dependencies": {
    "@prisma/client": "^6.19.2",
    "bcryptjs": "^3.0.3",
    "cors": "^2.8.5",
    "dotenv": "^17.2.3",
    "express": "^5.2.1",
    "google-auth-library": "^10.5.0",
    "jsonwebtoken": "^9.0.3",
    "prisma": "^6.19.2"
  }
}
